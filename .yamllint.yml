extends: default

rules:
  # Connection strings and image references cannot always be wrapped.
  line-length:
    max: 160
    level: warning
  document-start: disable
  comments:
    min-spaces-from-content: 1
  truthy:
    check-keys: false
